<Window x:Class="RRM_SM.UI.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="420" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText"
                   Foreground="#E0E0F0" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <TextBox Grid.Row="1" x:Name="InputTextBox"
                 Background="#22223A" Foreground="#E0E0F0"
                 BorderBrush="#3E3E5E" Padding="8,6" FontSize="13"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Width="80" Padding="0,6" Margin="0,0,8,0"
                    Background="#5A4A20" Foreground="#C9A84C" BorderBrush="#C9A84C" BorderThickness="1"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Padding="0,6"
                    Background="#3E3E5E" Foreground="#E0E0F0" BorderBrush="#5E5E7E" BorderThickness="1"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

